<div class="form-group">
    <label for="gender" class="fw-bolder">Género</label>
    <select class="form-select form-control mt-2" id="gender" name="gender">
        <option value="Macho"
                <% if (typeof pet !== "undefined" && pet.gender === "Macho") { %>selected
                <% } %>
        >Macho
        </option>

        <option value="Hembra"
                <% if (typeof pet !== "undefined" && pet.gender === "Hembra") { %>selected
                <% } %>
        >Hembra
        </option>
    </select>
</div>

<div class="form-group mt-3">
    <label for="name" class="fw-bolder">Nombre</label>
    <input id="name" type="text" class="form-control mt-2" name="name"
           <% if (typeof pet !== "undefined") { %>value="<%= pet.name %>"
            <% } %>
    >
</div>

<div class="form-group mt-3">
    <label for="breed" class="fw-bolder">Raza</label>
    <input id="breed" type="text" class="form-control mt-2" name="breed"
           <% if (typeof pet !== "undefined") { %>value="<%= pet.breed %>"
            <% } %>
    >
</div>

<div class="form-group mt-3">
    <label for="age" class="fw-bolder">Edad</label>
    <input id="age" type="text" class="form-control mt-2" name="age"
           <% if (typeof pet !== "undefined"){ %>value="<%= pet.age %>"
            <% } %>
    >
</div>